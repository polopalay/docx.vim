[package]
name = "docx"
version = "0.1.0"
edition = "2021"

[dependencies]
zip = { version = "0.6", default-features = false, features = ["deflate"] }
quick-xml = "0.31"

[profile.release]
opt-level = 3
lto = "thin"
